using System;
using System.Collections.Generic;
using System.Text;

namespace Dinamik_rotor
{
    class KompensirMass
    {
        private const double p = 7.82;
        private double m, M ;
        
        public double Shponka(double b, double h, double l)
        {
           // Weight M = new Weight();
            //m = M.Rectangle(p, b, h, l);
            return m;         
        }
    }
}
